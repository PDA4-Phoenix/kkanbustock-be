spring:
  datasource:
    url: jdbc:mysql://min-sang-eun-db.clemtdx79oxt.ap-northeast-2.rds.amazonaws.com:3306/kkanbustock
    username: admin
    password: kkanbustock

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false #osiv
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show-sql: true
        format_sql: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
jwt:
  header: Authorization
  secret: '62ee24aa5950496692f0c633e26b58b062ee24aa5950496692f0c633e26b58b01'
  token-validity-in-seconds: 86400

logging:
  level:
    org.springframework.orm.jpa: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.springframework.jdbc.core.JdbcTemplate: DEBUG
    org.springframework.transaction: DEBUG

server:
  servlet:
    encoding:
      charset: UTF-8
      force: true
  port: 8081